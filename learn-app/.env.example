# Enchiridion — public Supabase config (safe to expose; protected by RLS).
# Copy to .env.local for local dev, and set these in your host (e.g. Vercel).
# These are the working defaults; override to point at a different project.
NEXT_PUBLIC_SUPABASE_URL=https://opbsfqblkixfppninyrx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_FoeFBr5KGnZJ-OTbaR96tw_loZi3d6F
